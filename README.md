# Cookie House

Next.js 16 + TypeScript + Tailwind v4. Animação: Lenis, GSAP (ScrollTrigger), Motion, matter-js.

```bash
npm install
npm run dev -- -p 3100   # a porta 3000 costuma estar ocupada pelo WAHA
npm run build
```

Contexto, regras e decisões de marca em `CLAUDE.md`.
